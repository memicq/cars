spring:
  datasource:
    driver-class-name: "com.mysql.cj.jdbc.Driver"
    url: ${DB_ENDPOINT}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: none
server:
  port: 8080